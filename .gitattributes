# Normalize all text files to LF in the repository and on checkout. The binary
# runs in a Linux container and ships shell scripts that must stay LF.
* text=auto eol=lf

# Binary assets (never normalize).
*.png binary
*.ico binary
*.woff binary
*.woff2 binary
*.ttf binary
